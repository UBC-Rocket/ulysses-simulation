animate_flight: generate an animation based on current setup, for visualization

add_scenery & add_follow cam: create background and rocket following camera (the frames for it), only run it if you want to change background or frames

unweld_gimbal: be used when rebuild model, don't need to be touched most times

capture layout & apply_layout: use it only if you want to change layout. capture first and apply later