V1...DataFile: smiData for CAD model

build model: it is able to generate a Simulink for rocket control based on CAD model(for example if you lose root_param_gimbal you will need it). It refers to mb wiring (connect plant with controller) and layout gimbal (read the layout you set). Don't need to be touched most times. 

plant param: an intermediate product from build model. It is an simplified simscape based on CAD model